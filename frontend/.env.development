# Vite dev server (npm run dev) — uses vite.config.ts proxy to forward /api → localhost:5000
# Relative URLs mean "same origin as the page" — proxy handles the rest.
VITE_API_URL=/api
VITE_API_AUTH_URL=/api/auth
VITE_API_GOOGLE_ADS_URL=/api/google-ads
